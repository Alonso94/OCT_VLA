# Local configuration and agent metadata
.env
.env.*
!.env.example
.agents/
.codex/

# Python and build artifacts
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.venv*/
build/
dist/

# External dependencies and experiment artifacts. Anchored to the repo root
# (leading slash) so this does not also match src/oct_vla/data/ or
# tests/data/, which are tracked source, not generated datasets.
RoboTwin/
/data/
datasets/
assets/
outputs/
logs/
*.log

# Git history replaces manual backups
*.orig
* copy.py
*_backup.py
